# Backend API origin (no trailing slash).
# Dev: leave empty or keep default — Vite proxies /api (see vite.config.ts).
# Production build: leave empty for same-origin /api (nginx), or set an explicit origin.
VITE_API_URL=http://127.0.0.1:8000
